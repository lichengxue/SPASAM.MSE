% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{plot_ssb_performance}
\alias{plot_ssb_performance}
\title{Plot performance of SSB across models and levels}
\usage{
plot_ssb_performance(
  mods,
  is.nsim,
  main.dir,
  sub.dir,
  var = "SSB",
  level = c("global", "region"),
  width = 10,
  height = 7,
  dpi = 300,
  col.opt = "D",
  method = NULL,
  outlier.opt = NA,
  plot.style = "median_iqr",
  show.whisker = TRUE,
  ssb.ymin = NULL,
  ssb.ymax = NULL,
  use.n.years = NULL,
  new_model_names = NULL,
  base.model = NULL,
  total = NULL,
  period = c("last", "first"),
  start.years = NULL
)
}
\arguments{
\item{mods}{A list (or list of lists) of MSE model results.}

\item{is.nsim}{Logical; if `TRUE`, `mods[[realization]][[model]]` structure.}

\item{main.dir}{Character. Main directory for saving plots.}

\item{sub.dir}{Character. Subdirectory inside `main.dir`.}

\item{var}{Character. Name of SSB value column, default `"SSB"`.}

\item{level}{Character vector; one or both of `"global"`, `"region"`.}

\item{width, height}{Numeric. Plot size in inches.}

\item{dpi}{Numeric. Resolution for PNG.}

\item{col.opt}{Viridis option, e.g. `"D"`.}

\item{method}{`NULL`, `"mean"`, or `"median"`. If non-`NULL`, aggregate the
selected years for each (Model, Realization, Label) by mean/median before plotting.}

\item{outlier.opt}{Outlier shape for `geom_boxplot()` (use `NA` to hide).}

\item{plot.style}{`"boxplot"` or `"median_iqr"`.}

\item{show.whisker}{Logical; if `TRUE`, draw whiskers in `"median_iqr"` style.}

\item{ssb.ymin, ssb.ymax}{Optional y-axis limits.}

\item{use.n.years}{Integer; number of years in the window (first or last).}

\item{new_model_names}{Optional character vector of replacement model names.}

\item{base.model}{Optional baseline model name for relative differences.}

\item{total}{Ignored for SSB (kept only so your plot_mse calls don't error).}

\item{period}{`"last"` or `"first"`; determines which part of the time series
is used.}

\item{start.years}{Integer or `NULL`. Starting index for `"first"` period.
If `NULL` and `period = "first"`, defaults to 1.}
}
\value{
A named list of ggplot objects, one per requested level.
}
\description{
Create boxplot or median–IQR summaries of spawning stock biomass (SSB)
performance across estimation models, optionally comparing to a baseline
model and summarizing either the **last** or the **first** N years.
Supports plotting at the global or region level and returns a list of
ggplot objects (one per requested level).

If a global option `mse_summary` is available (as created elsewhere in
your workflow), the function will use pre-computed SSB summaries from that
object. Otherwise, it will call `extract_mods_SSB()` on `mods` to derive
the SSB data.
}
