% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{export_mse_performance_statistics}
\alias{export_mse_performance_statistics}
\title{Export MSE performance statistics used by the plotting wrapper}
\usage{
export_mse_performance_statistics(
  mods,
  is.nsim,
  main.dir = getwd(),
  sub.dir = "Report",
  output_file = "mse_performance_statistics.csv",
  mse_summary = getOption("mse_summary", NULL),
  method = "mean",
  start.years = 1,
  use.n.years.first = 5,
  use.n.years.last = 5,
  new_model_names = NULL,
  base.model = "Model1",
  include_relative = !is.null(base.model),
  include_status = TRUE,
  include_kobe = TRUE,
  include_holistic = TRUE,
  include_diagnostics = TRUE
)
}
\arguments{
\item{mods}{Model outputs list. For non-simulation runs:
\code{list(Mod1, Mod2, ...)}. For simulation runs:
\code{list(sim1=list(Mod1, Mod2, ...), sim2=list(...), ...)}.}

\item{is.nsim}{Logical; whether \code{mods} is nested by simulation
realization.}

\item{main.dir}{Character. Top-level output directory.}

\item{sub.dir}{Character. Subdirectory under \code{main.dir}.}

\item{output_file}{Character or \code{NULL}. File name or path for the CSV.
If \code{NULL}, the data frame is returned without writing a file.}

\item{mse_summary}{Optional precomputed summary from \code{build_mse_summary()}.}

\item{method}{Character or \code{NULL}. Same year-aggregation method used by
the plotting functions.}

\item{start.years}{First row index used for short-term windows.}

\item{use.n.years.first, use.n.years.last}{Window lengths used for short- and
long-term summaries.}

\item{new_model_names}{Optional display names for models.}

\item{base.model}{Optional baseline model used for relative performance plots.}

\item{include_relative}{Logical; include relative-to-baseline rows when
\code{base.model} is supplied.}

\item{include_status, include_kobe, include_holistic}{Logical; include the
corresponding plot families.}

\item{include_diagnostics}{Logical; include diagnostic parameter rows for
mean recruitment, recruitment sigma, and NAA sigma from the final EM.}
}
\value{
A data frame. If \code{output_file} is not \code{NULL}, the written
  path is stored in \code{attr(result, "file")}.
}
\description{
Builds a tidy CSV containing the raw values and distribution summaries used by
the performance, status, Kobe, annual-variation, and holistic plots generated
by \code{plot_mse_output()}. Holistic rows are reported before any max-min
normalization is applied.
}
