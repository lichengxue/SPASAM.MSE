% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{plot_catch_performance}
\alias{plot_catch_performance}
\title{Plot performance of Catch across models and levels}
\usage{
plot_catch_performance(
  mods,
  is.nsim,
  main.dir,
  sub.dir,
  var = "Catch",
  level = c("global", "region", "fleet"),
  width = 10,
  height = 7,
  dpi = 300,
  col.opt = "D",
  method = NULL,
  outlier.opt = NA,
  plot.style = "median_iqr",
  show.whisker = TRUE,
  f.ymin = NULL,
  f.ymax = NULL,
  use.n.years = NULL,
  new_model_names = NULL,
  base.model = NULL,
  total = FALSE,
  period = c("last", "first", "all"),
  start.years = NULL
)
}
\arguments{
\item{mods}{A list (or list of lists) of MSE model results.}

\item{is.nsim}{Logical; whether \code{mods} is nested over realizations.}

\item{main.dir}{Character. Main directory for saving plots.}

\item{sub.dir}{Character. Subdirectory (inside \code{main.dir}) where plots
will be saved under \code{Performance_Boxplot}.}

\item{var}{Character. Value column name (default \code{"Catch"}).}

\item{level}{Character vector: \code{"global"}, \code{"region"}, \code{"fleet"}.}

\item{width, height}{Numeric plot size (inches).}

\item{dpi}{Numeric resolution.}

\item{col.opt}{Viridis option.}

\item{method}{\code{NULL}, \code{"mean"}, or \code{"median"}.}

\item{outlier.opt}{Outlier shape for boxplot (use \code{NA} to hide).}

\item{plot.style}{\code{"boxplot"} or \code{"median_iqr"}.}

\item{show.whisker}{Logical; show whiskers for median–IQR style.}

\item{f.ymin, f.ymax}{Numeric or \code{NULL}; included for interface consistency.}

\item{use.n.years}{Integer or \code{NULL}; used for \code{first} / \code{last}.}

\item{new_model_names}{Optional model relabeling vector.}

\item{base.model}{Optional baseline model for relative plots.}

\item{total}{Logical; if \code{TRUE} sum over selected period.}

\item{period}{One of \code{"last"}, \code{"first"}, or \code{"all"}.}

\item{start.years}{Integer start year for \code{period="first"}.}
}
\value{
Named list of ggplot objects (one per requested level).
}
\description{
Create publication-ready boxplot or median–IQR summaries of realized Catch
across estimation models, optionally comparing to a baseline model and
summarizing either the **first**, **last**, or **all** years in the time
series. Catch can be plotted at the global, region, or fleet level, and
either on a per-year basis or as a **total sum** over the chosen period.

If a global option \code{mse_summary} is available (as created by
\code{build_mse_summary}), the function may use pre-computed Catch summaries
from that object for \code{period = "first"} or \code{period = "last"}.
For \code{period = "all"}, this function **always extracts from mods**
to guarantee all years are included (avoids the case where cached summaries
are already truncated).

IMPORTANT FIX (minimal):
\itemize{
  \item Saved filenames include \code{"_perf"} so your report filters
        (grepl("perf", ...)) can detect these plots.
  \item \code{period="all"} now truly uses all years (bypasses \code{mse_summary}).
}
}
