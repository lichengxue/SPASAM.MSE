% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{plot_kobe_status}
\alias{plot_kobe_status}
\title{Plot Kobe-style stock status (SSB/SSB_X% vs F/F_X%).}
\usage{
plot_kobe_status(
  mods,
  is.nsim,
  main.dir,
  sub.dir,
  width = 10,
  height = 7,
  dpi = 300,
  col.opt = "D",
  new_model_names = NULL,
  use.n.years = NULL,
  period = c("last", "first"),
  start.years = 1,
  plot_style = c("both", "points", "density"),
  nbins = 20
)
}
\arguments{
\item{mods}{List of models (or list over realizations if is.nsim=TRUE).}

\item{is.nsim}{Logical. TRUE if mods is realizations x models.}

\item{main.dir}{Character. Main output directory where plots will be saved.}

\item{sub.dir}{Character. Subdirectory under main.dir (KOBE_Plot created inside).}

\item{width, height}{Numeric. Plot size in inches for ggsave().}

\item{dpi}{Numeric. DPI for ggsave().}

\item{col.opt}{Character. Viridis option.}

\item{new_model_names}{Optional character vector of display names for models.}

\item{use.n.years}{Integer. Window length. Default 1.}

\item{period}{Character. "last" or "first".}

\item{start.years}{Integer. 1-based start index for period="first".}

\item{plot_style}{Character. "both", "points", or "density".}

\item{nbins}{Integer. Density bin levels when plot_style includes "density".}
}
\value{
Invisibly returns a list of ggplot objects (base panel per stratum).
        Files are saved to file.path(main.dir, sub.dir, "KOBE_Plot").
}
\description{
Computes stock status ratios using OM reference points and makes Kobe-style
phase plots by region and globally. Supports plotting the last N years or a
first-window of N years starting at start.years. Can save points, density,
or both styles to a single output folder.
}
